#include<iostream>
using namespace std;

int main() {
/*
// Print 1 to n

    int n;
    cin >> n;
    int i = 1;

    while (i<=n) {
        cout << i << "\n";
        i = i + 1;
    }

*/ 

/*
// Sum of 1 to n

    int n;
    cout << "Value of n: ";
    cin >> n;
    int i = 1;
    int sum = 0;

    while (i<=n) {
        sum = sum + i;
        i = i + 1;
    }
    cout<<"Sum of 1 to n:" << n << " = " << sum;
*/


/*
// Sum of even no from 1 to n
int n;
    cout << "Value of n: ";
    cin >> n;
    int i = 0;
    int sum = 0;

    while (i<=n) {
        sum = sum + i;
        i = i + 2;
    }
    cout<<"Sum of even no from 1 to n:" << n << " = " << sum;
*/

}